# 设备联调记录（待真机执行）

当前只验收软件、测试替身和 Android 编译。不得将下表空项写成已通过；不烧写固件，不承诺锁屏期间持续采集。

## 环境

- APK 提交 / SHA256：
- 手机型号 / Android 版本 / 中文识别和 TTS 服务版本：
- ESP32 固件 / MLX90614 型号 / JDY-31：
- 场景、时间、测试者：
- 通知权限 / 精确计时权限 / 系统电池限制：

## 操作与证据

| 场景 | 操作 | 应观察到的行为 | 结果与诊断文件 |
|---|---|---|---|
| 权限拒绝 | 拒绝附近设备或麦克风 | 明确提示，保留文字/按钮；不假装已连接 | 待实测 |
| 配对 | 系统配对 JDY-31，再在 App 扫描连接 | 真实连接状态；新窗口先待稳定 | 待实测 |
| 持续传输 | 静止对准室内物体 10 分钟，切库存页再返回 | 不因切页主动断开；不重复帧监听 | 待实测 |
| 断线恢复 | 断模块电源，再恢复供电 | 断线/重连记录；旧温度不作为实时值 | 待实测 |
| 蓝牙关闭 | 手机关闭蓝牙并重新打开 | 蓝牙状态明确；必要时用户重新连接 | 待实测 |
| 锁屏恢复 | 开始 90 秒计时，锁屏两分钟 | 剩余按实际时间；不自动跳步；核对通知是否一次 | 待实测 |
| 精确权限未授予 | 开启提醒但不授予精确计时 | 显示可能延迟，恢复前台有状态卡 | 待实测 |
| 中文语音 | 各说一次七类指令，取消一次识别 | 仅最终结果；不确定先确认；收音/播报互斥 | 待实测 |
| 用户切换 | 计时运行时退出，另一账号登录 | 取消旧通知/音频；恢复原账号需确认 | 待实测 |

## 测温条件记录

| 时间 | 目标材质/表面 | 距离(cm) | 角度(°) | 视场是否覆盖目标 | 静止/移动/遮挡 | 参考仪器及校准情况 | 温度与异常 | 日志 |
|---|---|---|---|---|---|---|---|---|
| 待填写 | | | | | | | | |

不要用锅面红外温度判断内部熟度或反推实际火力。先记录静止室温物体，再做不同材质/角度/距离对照。没有参考仪器时只报告连续性、丢帧和重复性，不报告测温准确度。

AI 厨房 →「设备状态与诊断」→ 导出设备诊断。文件保留最多 2000 条原始帧、解析样本和连接事件；长测试分段导出。浏览器导出会标为 `browser-no-hardware`，仿真回放另用温度卡片的导出入口。

离线汇总：`python scripts/summarize-device-diagnostics.py <导出文件.json>`。输出仅描述日志，不代替真实硬件验收。
