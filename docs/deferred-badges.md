# 徽章与奖励去重

第一餐记录、菜谱探索、一周坚持三个预置徽章分别依据已确认完成、三种菜谱、已加入的一周挑战达标。首次完成/探索允许用户明确确认导入的旧记录；周挑战仍排除旧记录导入。

每账号/徽章定义为唯一数据库记录，保存规则版本、发放时间与完成事件或挑战 ID。完成同步与发奖在同一事务中；读取徽章时可修复版本升级前已满足条件但尚未发放的记录，不重复奖励。GET /api/v3/badges 仅返回本人数据。

验证：重复完成回调不重复发放，三类条件、账号隔离、注销后会话失效、新登录恢复完全一致；移动页面刷新/重载后只有一份徽章；前端构建通过。徽章是参与记录，不是技能认证。
