# 离线分支更新（2026-09-24）

正式版已通过 PR #27 合入 main（4ca38e3）。本分支单向同步正式代码，新增确认识别与批次扣减本地适配、持久模拟凭证及 B/D/N 目标保存。应用 ID 独立、版本号 4 / 1.2.0-localtest。源分支只上传，不合入 main。

验证：68 项通用前端测试、8 项离线运行时测试；18 项浏览器场景覆盖识别示例→库存→推荐、计时/语音按钮、调整、用户切换、回放/WASM、批次选择、响应丢失后凭证查询。测试主动阻断全部 API/外部请求，观测请求数为 0，页面未捕获错误为 0。推荐流程在浏览器断网状态下通过。

APK 的准确源提交、SHA256 和 Android 构建日志随发布产物提供。浏览器/构建通过不代表真实手机 ASR、中文 TTS、锁屏通知已验收。已有用户反馈 JDY-31 可连接并显示温度；本次未连接手机复验。

样例鲜度和推荐只是固定展示，不伪称真实算法效果。必要库存服务仍在正式 main；离线包仅用本机数据模拟交互，卸载或重置测试账号会丢失该测试数据。已有无凭证旧扣减记录需要人工核对或在确认可丢弃测试数据后重置账号，不会自动重扣。

下载：GitHub Releases 的 `offline-test-20260924`，或本分支独立 Actions 产物。下载发布页 APK 可保持本地已验证签名；不要把离线测试记录当作正式库存。
